Toilet Plus brand lockup — the "+" droplet mark with optional Archivo wordmark; use on any header, footer, or hero.

```jsx
<Logo size={40} href="/" />
<Logo tone="dark" />          // on navy footer / photo
<Logo showWordmark={false} size={32} />  // compact / favicon-style
```

Variants: `tone` light|dark, `showWordmark`, `size` (mark px; wordmark scales). Keep clear space ≥ half the mark height around the lockup.
